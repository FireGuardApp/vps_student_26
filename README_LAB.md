# VPS SOC Analyzer - работа на Python

Добро пожаловать в репозиторий лабораторной работы по разработке модульного инструмента автоматизации информационной безопасности (мини-SOC) для VPS-сервера!

В рамках  работы: написание сетевых утилит до парсинга реальных логов угроз, оценка уровня риска сервера, контроль целостности его конфигурации и публикации своего проекта на GitHub по стандартам разработки (GitHub Flow).

---

## 🎯 Цели и задачи работы

Вам предстоит реализовать набор функций в модуле `vps_soc_analyzer.py` и интегрировать их в общую цепочку анализа (Pipeline) в файле `vps_soc_main.py`.

### 🛠️ Что нужно реализовать:
1. **Парсинг логов SSH (`extract_ip`)**: Извлечение IP-адресов злоумышленников из строк о неудавшихся попытках входа.
2. **Агрегацию атак (`group_by_ip`)**: Подсчет количества атак по каждому IP-адресу с помощью словарей.
3. **Детектор Brute-Force (`detect_brute_force`)**: Выявление IP-адресов, превысивших заданный порог атак.
4. **Анализ веб-логов Nginx (`detect_suspicious_paths`)**: Поиск следов веб-сканирования (LFI, обход путей, поиск `.env`).
5. **Оценку уровня рисков (`calculate_risk_score`)**: Вычисление балла опасности и выставление статуса (`LOW`, `MEDIUM`, `HIGH`).
6. **Сетевой сканер портов (`is_port_open`)**: Написание сокетного детектора открытых TCP-портов.
7. **Контроль целостности файлов (`get_file_hash`)**: Вычисление SHA-256 хеш-суммы для системных конфигураций.

---

## 📂 Структура проекта

Ваш проект на GitHub должен содержать следующие файлы:
* `vps_soc_analyzer.py` — основной модуль, в котором вы реализуете логику анализаторов (замените `pass` в шаблоне на работающий код).
* `vps_soc_main.py` — управляющий конвейер (Pipeline). Вам нужно раскомментировать вызовы разработанных функций в соответствующих шагах.
* `test_vps_soc.py` — набор модульных тестов (`unittest`) для автоматической проверки вашего кода.
* `vps_log_generator.py` — скрипт генерации искусственных логов (не подлежит изменению).
* `requirements.txt` — зависимости проекта.
* `README.md` — документация к вашему проекту.

---

## 🚀 Пошаговое руководство по выполнению и сдаче работы

### Шаг 1. Форк (Fork) репозитория
1. В правом верхнем углу этой страницы на GitHub нажмите кнопку **Fork**.
2. Создайте копию проекта на вашем аккаунте GitHub.

### Шаг 2. Клонирование и создание персональной ветки
Откройте терминал **Git Bash** на своём компьютере и клонируйте ваш форк:
```bash
# Клонирование репозитория (замените [ВАШ_НИК] на ваш логин на GitHub)
git clone https://github.com/[ВАШ_НИК]/vps-soc-analyzer.git
cd vps-soc-analyzer

# Создание персональной ветки (имя ветки в формате фамилия-имя)
git checkout -b ivanov-petr
```

### Шаг 3. Разработка функций аналитика
Откройте проект в вашей любимой среде разработки (VS Code, PyCharm, IDLE) и реализуйте пустые функции в файле `vps_soc_analyzer.py`, ориентируясь на подсказки и документацию внутри функций.

> ⚠️ **Требование к коду:** Все функции должны содержать аннотации типов (Type Hints) для параметров и возвращаемых значений, использовать только локальные переменные для обеспечения безопасности потоков и избегать глобальных состояний.

### Шаг 4. Настройка конвейера
Откройте файл `vps_soc_main.py` и раскомментируйте строки вызовов ваших функций на Шагах 2, 3, 4, 5 и 6, чтобы интегрировать их в общую логику обработки.

### Шаг 5. Модульное тестирование
Перед деплоем кода на реальный сервер, запустите модульные тесты на вашем компьютере:
```bash
python -m unittest test_vps_soc.py
```
Все тесты должны завершиться со статусом **`OK`**. Если какой-то тест выдает `FAIL` или `ERROR`, исправьте ошибку в вашем коде.

### Шаг 6. Деплой и проверка работы на учебном VPS-сервере
1. Подключитесь к нашему серверу-полигону по SSH под профилем `student`:
   ```bash
   ssh student@5.180.173.78
   ```
   *(Пароль выдается преподавателем).*
2. Скопируйте ваши измененные файлы `vps_soc_analyzer.py` и `vps_soc_main.py` на сервер с помощью утилиты `scp` (запускать из Git Bash локально) или SFTP-клиента (например, FileZilla):
   ```bash
   # Выполняется ЛОКАЛЬНО в папке проекта
   scp vps_soc_analyzer.py vps_soc_main.py student@5.180.173.78:/home/student/
   ```
3. Выполните запуск конвейера на VPS и убедитесь, что он выводит красивый структурированный консольный отчет с реальными логами:
   ```bash
   # Выполняется на VPS
   python3 vps_soc_main.py
   ```

### Шаг 7. Коммит и создание Pull Request
Когда всё работает идеально локально и на VPS, зафиксируйте ваш прогресс в Git и отправьте ветку на GitHub:
```bash
git add vps_soc_analyzer.py vps_soc_main.py
git commit -m "Выполнил задания 1-7. Все тесты пройдены успешно."
git push -u origin ivanov-petr
```
Зайдите в ваш форкнутый репозиторий на GitHub, нажмите кнопку **Compare & pull request**, заполните описание работы и отправьте запрос на слияние в репозиторий преподавателя.

---

## 📊 Критерии оценки (Чек-лист для сдачи)

Каждая функция оценивается отдельно:
* **[10 баллов]** — Функция `extract_ip`: правильность регулярного выражения, обработка пустых значений, наличие аннотаций типов.
* **[10 баллов]** — Функция `group_by_ip`: корректная логика агрегации через словарь.
* **[15 баллов]** — Функция `detect_brute_force`: правильная фильтрация по порогу атак.
* **[15 баллов]** — Функция `detect_suspicious_paths`: правильное приведение к регистру, корректная проверка сигнатур.
* **[15 баллов]** — Функция `calculate_risk_score`: математическая логика расчета и присвоение категорий рисков.
* **[15 баллов]** — Функция `is_port_open`: надежность обработки сокетного подключения, своевременный вызов `.close()` (или использование `with`) и обработка таймаутов.
* **[10 баллов]** — Функция `get_file_hash`: поблочное чтение файлов, обработка ошибки `FileNotFoundError`.
* **[10 баллов]** — Общая интеграция: работоспособность тестов `test_vps_soc.py` и запуск сквозного конвейера на VPS.

*Максимальный балл за лабораторную работу — 100 баллов.*
